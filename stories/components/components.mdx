import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Components/Components" />

# Component Guidelines

The components featured under the examples sections are simplified for Storybook.

## Picking Example Components

1. The component should be something we frequently use.
2. Avoid components with a project specific use-case.
